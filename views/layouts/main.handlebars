<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tech Blog</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-9ndCyUaIbzAi2FUVXJi0CjmCapSmO7SnpJef0486qhLnuZ2cdeRhO02iuK6FUUVM" crossorigin="anonymous">
</head>
<body>
    <header class="bg-secondary text-white">
        <div class="container d-flex flex-column flex-md-row align-items-center p-4">
            <h1 class="flex-grow-1">My Tech Blog</h1>
            <a href="/" class="btn btn-secondary">Home</a>
            <a href="/dashboard" class="btn btn-secondary">Dashboard</a>
            <a id="logInButton" href="/login" class="btn btn-secondary" style="display: {{#if userId}}none{{else}}block{{/if}}">Log In</a>
            <button id="logOutButton" type="button" class="btn btn-secondary" style="display: {{#if userId}}block{{else}}none{{/if}};">Log Out</button>
        </div>
    </header>
    <main class="container mt-4">
        {{{ body }}}
    </main>
    <script src="/js/logoutScript.js"></script>
</body>
</html>